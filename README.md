# CPP-Project
Login and Registration System
A pretty uncomplicated login and registration system with most of the validations and checking. 
It reads user information (username and password) from a text file, allowing users to either sign up for a new account or log in to an existing one.
